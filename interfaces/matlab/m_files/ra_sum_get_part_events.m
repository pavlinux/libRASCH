function  ra_sum_get_part_events(sum_handle, part_id)
% Returns for an event-summary part the event-ids which are the part is based on.
%   Input parameters:
%     sum_handle
%     part_id
% 
%   Output parameters:

	 ra_matlab('ra_sum_get_part_events', sum_handle, part_id);
