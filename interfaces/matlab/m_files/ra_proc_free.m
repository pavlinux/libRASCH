function  ra_proc_free(process_handle)
% The function ra_proc_do() allocates some memory to
% store the results. This function frees the allocated memory.
%   Input parameters:
%     process_handle
% 
%   Output parameters:

	 ra_matlab('ra_proc_free', process_handle);
